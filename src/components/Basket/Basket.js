import React from 'react';
import {
  HeaderElement,
} from '../../styles';

export const Basket = ({ totalCount }) => <HeaderElement>
  Star Wars
      <div>BASKET: {totalCount}</div>
</HeaderElement>;
